# 网页样式检查

检查日期：2026-09-08。

## 修改边界

生产代码仅修改 `web/style.css` 和 `web/mission/src/App.css`。不修改模块归属、数据、事件处理、补丁生成、导入导出或校验逻辑。

## 已完成

- [x] 统一内外页面的字号、控件高度、间距、焦点和禁用状态。
- [x] 压缩重复标题和留白；恢复被通用样式误隐藏的存档队伍编号。
- [x] 中等宽度下，任务与队伍列表上下排列，右侧编辑区域独立滚动。
- [x] 窄屏战术条件、装备和长名称自动换行，标签栏横向滚动。
- [x] 修正下拉菜单底部裁切、清空按钮被长文本撑高的问题。
- [x] 统一深色表面、边框和警告色；手机输入框使用适合触摸的尺寸。
- [x] 保留现有交互和模块分组，无新增依赖。

## 验证

使用 Microsoft Edge 的 Playwright 自动化，覆盖 8 个分类、17 个 MOD 子页、空存档入口和 5 个存档编辑页。测试组合为 1440×900 简体中文浅色、1024×768 英文浅色、390×844 简体中文浅色、1440×900 日文深色。

`web/browser-style-audit.mjs` 共输出 159 张截图，检查横向溢出、控件越界、下拉菜单边界、展开说明及长页面中段和底部；结果无发现、无页面错误。截图保存在忽略目录 `.tools/browser-validation/style-audit/`，并人工抽查主要布局。

另通过 52 项核心测试、两个 Web 构建，以及 `browser-smoke.mjs`、`browser-locale-layout.mjs`、`browser-runtime.mjs` 三组回归，包含语言切换、编辑保留、独立滚动、工程保存和实际导出文件校验。

复现样式检查（先启动本地预览）：

```sh
AUDIT_STRICT=1 BROWSER_EXECUTABLE='/Applications/Microsoft Edge.app/Contents/MacOS/Microsoft Edge' TEST_URL=http://127.0.0.1:4181/ node web/browser-style-audit.mjs
```

## 验证边界

本次浏览器实测使用 Edge，不代表已覆盖所有浏览器。下拉菜单优先使用原生 CSS 锚点定位以适配剩余空间；不支持该能力的浏览器保留原来的绝对定位回退样式。测试使用合成存档，不修改用户原存档；不涉及模拟器或游戏内效果验证。
